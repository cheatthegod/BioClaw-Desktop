Make BioClaw Desktop (/lambda/nfs/file2/cqr_files/Bioclaw_paper/BioClaw-Desktop-v2; Tauri2 + React + Rust sidecar) a COMPLETE native client of the BioClaw SaaS (chat.bioclaw.tech; prod source /home/ubuntu/Bioclaw_dev/BioClaw-SaaS): every website feature usable from the desktop, PLUS local-first (offline skills, bundled Python env, local inference).

STEP 1 — read docs/GOAL_full_parity.md in the desktop repo. It is the authoritative spec (architecture, full feature matrix, every milestone's detail + acceptance criteria). Follow it exactly; this is only the compressed driver. Maintain docs/PARITY_STATUS.md every milestone (one row per feature: not-started/in-progress/done/N-A+reason).

ARCHITECTURE (fixed): desktop = native client. Keystone FIRST: a generic authenticated SaaS proxy in the Rust sidecar — ANY /saas/*path → <SAAS_BASE>/api/*path, attaching the keychain device-code session cookie, streaming SSE through, 401→{error:"auth",loginUrl}. Then every feature is a React panel calling the local proxy via a typed client (src/lib/api/saas.ts + useSaasQuery/useSaasStream). Already DONE, don't redo: sidecar Stage L.1–L.7 (health, skills, env, /chat+tools, permissions, device-code auth).

GUARDRAILS (never violate): install to files2 not /home/ubuntu; never commit .signing-keys or any key/cert; never send real API keys externally (SaaS holds them; desktop auths as a user via device-code); never restart the prod `bioclaw` systemd service without explicit OK; keep the sage palette+fonts; bilingual zh-CN/en. Each milestone ends GREEN (sidecar: cargo build --release + cargo clippy --all-targets -D warnings + cargo test + fmt; frontend: npm run build) and is ONE commit whose body lists what was verified by RUNNING the built app — "compiles" is NOT "works". Unknown endpoint shape → read BioClaw-SaaS/src/channels/local-web/channel.ts, don't guess.

MILESTONES — strict order, one commit each, don't start one until prev is committed+green (detail in the doc):
- Phase 0: M0.1 persist device session token in OS keychain; M0.2 MCP client + JSON-RPC framing; M0.3 ship the Rust sidecar as a Tauri externalBin and drop the Node sidecar; M0.4 SQLite memory tools; M0.5 CI 3-platform build matrix green.
- Phase 1 keystone: M1.1 generic authed streaming SaaS proxy; M1.2 typed frontend client + hooks.
- Phase 2 panels P0→P1→P2: M2.1 GPU Tools panel FIRST (list/group tools, dynamic param + file-input form, submit, live-stream job log, cancel, download, host-status badge) — ACCEPT = from the built app submit a real rnagenesis-design (4 seqs) AND foldmark-watermark (short peptide) and watch both reach `done` with downloadable FASTA/CIF (verified server-side 2026-06-29). Then in order: chat history, skills center (local+SaaS), KB search, quota, projects+datasets+files, sharing, profile/account/feedback, paper-digest, contacts, lab (read-mostly), manage, admin (gated to admins).
- Phase 3 local-first: M3.1 local GPU inference option when a local conda env exists (default cloud); M3.2 offline mode (local chat+skills+env keep working with a clear cloud-unavailable banner); M3.3 native notifications + tray + deep links + drag-drop.
- Phase 4 release: M4.1 theming parity; M4.2 i18n zh-CN+en; M4.3 two-tier auto-update; M4.4 code signing + per-platform installers (keys from CI secrets only); M4.5 e2e acceptance on mac/win/linux.

DONE WHEN: a signed installer exists for mac+win+linux; fresh install → device-code sign-in → no matrix feature 404s/dead-ends (P0/P1 work against the live SaaS, P2 render + do their primary action, admin gated); RNAGenesis+FoldMark runnable from the GPU panel with downloadable outputs; works offline for local chat+skills+env with a banner; UI bilingual and matches the web; clippy -D warnings + cargo test + npm run build green; CI builds all 3 platforms; no secret committed; no production service disrupted without confirmation.
